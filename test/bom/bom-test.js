const assert = require('assert');
const bomModule = require('../../src/bom/index');

describe('#bom.js', () => {
    describe('#browser()', () => {
        it('browser() should return true', () => {
          // assert.strictEqual(arrModule.browser(arr1, arr2), true);
        });
    });
});